# HashTrust Assets

1. Export optimized screenshots and name them sequentially (`image-1.svg`, `image-2.png`, ...).
2. Keep file sizes reasonable (<1MB) and prefer SVG/WEBP where possible.
3. Update project data if you change filenames beyond the default `image-1` reference.
