# ShoeMania Assets

- Store your ecommerce UI screenshots as `image-1`, `image-2`, ... (SVG/PNG/WebP).
- Keep consistent aspect ratios (16:9 recommended) for clean rendering.
- Update the data files if you rename the default `image-1`.
